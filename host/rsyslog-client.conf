module(load="imuxsock")  # локальные сообщения из /dev/log
module(load="imklog")    # системные сообщения ядра
module(load="omfwd")

*.* action(
  type="omfwd"
  target="@@RSYSLOG_SERVER@@"
  port="514"
  protocol="udp"
)